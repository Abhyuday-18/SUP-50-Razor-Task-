1st Prompt :-
We are building a working backend project. Create the entire project structure with all files in place and working. Here are the exact requirements:
Stack:

Backend: Express.js (JavaScript only, no TypeScript)
Database: PostgreSQL (hosted on Supabase, connection via DATABASE_URL)
ORM: Drizzle ORM for all queries
Migrations: Drizzle Kit

Port: The server must run strictly on port 7002. Read it from .env as PORT but default must be 7002.
Folder structure — create every file listed:
razorpay-reimbursements/
  package.json
  .env
  drizzle.config.js
  prompt-logs.txt
  src/
    server.js
    app.js
    config/
      db.js
      env.js
    db/
      schema/
        users.js
        employeeManager.js
        reimbursements.js
        reimbursementApprovals.js
        index.js
      migrations/
      seed.js
    middlewares/
      authenticate.js
      authorize.js
      errorHandler.js
    modules/
      auth/
        auth.routes.js
        auth.controller.js
        auth.service.js
      roles/
        roles.routes.js
        roles.controller.js
        roles.service.js
      employees/
        employees.routes.js
        employees.controller.js
        employees.service.js
      reimbursements/
        reimbursements.routes.js
        reimbursements.controller.js
        reimbursements.service.js
    utils/
      AppError.js
      responseHelper.js
package.json scripts — these must be exactly named:

dev — nodemon src/server.js — starts the app on port 7002
db:generate — drizzle-kit generate — generates migration files from schema (used during dev)
db:migrate — drizzle-kit migrate — applies migrations to the database (this is what the tester runs)
db:seed-data — node src/db/seed.js — seeds the CFO account only, nothing else

Dependencies to install:

express — HTTP framework
drizzle-orm — ORM for all DB queries
postgres — PostgreSQL driver used by drizzle-orm
jsonwebtoken — signs and verifies JWT tokens
bcryptjs — hashes passwords before storing, verifies on login (use bcryptjs not bcrypt, no native compilation issues)
cookie-parser — parses httpOnly cookies so Express can read the JWT token
cors — cross-origin headers, must be configured with credentials: true and origin explicitly set (not wildcard *) so cookies are sent correctly
morgan — HTTP request logger, use dev format
dotenv — loads .env file into process.env at startup

Dev dependencies:

drizzle-kit — CLI for generating and running migrations
nodemon — auto-restarts server on file changes

Environment variables — create .env with these keys as placeholders:
DATABASE_URL=
JWT_SECRET=
PORT=7002
File responsibilities:
src/server.js — entry point only. Imports app, starts listening on port 7002.
src/app.js — sets up Express, registers all middleware (cors with credentials:true, morgan, cookie-parser, express.json), mounts all module routers under the /rest prefix.
src/config/env.js — validates that DATABASE_URL and JWT_SECRET exist in process.env at startup. If either is missing, throw an error immediately and stop the server.
src/config/db.js — creates and exports the drizzle database instance using the postgres driver and DATABASE_URL.
drizzle.config.js — drizzle-kit config. Schema path points to src/db/schema/index.js, migrations output to src/db/migrations/.
Database schema — create these 4 tables in their respective files:
src/db/schema/users.js — table users with columns: id (uuid, primary key, default gen_random_uuid()), name (text, not null), email (text, not null, unique), password_hash (text, not null), role (text, not null, default 'EMP'), created_at (timestamp, default now()).
src/db/schema/employeeManager.js — table employee_manager with columns: id (uuid, primary key), emp_id (uuid, FK to users.id), rm_id (uuid, FK to users.id), assigned_at (timestamp, default now()).
src/db/schema/reimbursements.js — table reimbursements with columns: id (uuid, primary key), emp_id (uuid, FK to users.id), title (text, not null), description (text), amount (numeric, not null), rm_approved (boolean, default false), ape_approved (boolean, default false), status (text, default 'PENDING'), created_at (timestamp, default now()), updated_at (timestamp, default now()).
src/db/schema/reimbursementApprovals.js — table reimbursement_approvals with columns: id (uuid, primary key), reimbursement_id (uuid, FK to reimbursements.id), approver_id (uuid, FK to users.id), approver_role (text, not null), decision (text, not null), decided_at (timestamp, default now()).
src/db/schema/index.js — exports all four schemas together.
Seed file:
src/db/seed.js — inserts exactly one user, the CFO account. Email: cfo@org.com, password: CFO#ORG@April2026 (bcrypt hash it before inserting), role: CFO, name: CFO. Do not seed anything else. The script must be idempotent — if the CFO already exists, skip insertion without error.
Middleware files:
src/middlewares/authenticate.js — reads the JWT from an httpOnly cookie named exactly token. Verifies it using JWT_SECRET. Attaches the decoded user payload to req.user. If cookie is missing or token is invalid, respond with 401.
src/middlewares/authorize.js — a factory function that takes an array of allowed roles and returns an Express middleware. Checks req.user.role against the allowed roles. If not allowed, respond with 403.
src/middlewares/errorHandler.js — global Express error handler (4 arguments). Handles AppError instances and unexpected errors. Always responds with JSON.
Utils:
src/utils/AppError.js — a custom error class extending Error. Constructor takes message and statusCode.
src/utils/responseHelper.js — exports two functions: sendSuccess(res, data, statusCode=200) sends { status: 'success', data } and sendError(res, message, statusCode) sends { status: 'error', message }.
Module files — create route, controller, service for each module. Leave controller and service functions as stubs (empty functions with a TODO comment) for now. Only wire up the routes:
All routes are under /rest prefix (mounted in app.js).
auth module routes:

POST /rest/onboardings/register
POST /rest/onboardings/login
POST /rest/onboardings/logout

roles module routes:

POST /rest/roles/assign

employees module routes:

GET /rest/employees
POST /rest/employees/assign
DELETE /rest/employees/assign

reimbursements module routes:

POST /rest/reimbursements
PATCH /rest/reimbursements
GET /rest/reimbursements
GET /rest/reimbursements/:userId

Other:
prompt-logs.txt — create this file at the root. Leave it empty for now.
Node version required: >= 20.10.2. Add engines field in package.json specifying this.
Do not use TypeScript. Do not use ESM imports — use CommonJS (require/module.exports) throughout.4




2nd Prompt:-

Now that the folder structure and scaffold are in place, implement the business logic for the following modules. Do NOT touch any reimbursement logic yet — leave those controller and service stubs as they are.
Roles & Hierarchy:
There are 4 roles: EMP, RM, APE, CFO. Every new registered user gets EMP by default. The CFO is the only one who can change roles.

AUTH MODULE — implement fully
POST /rest/onboardings/register

Request body: name, email, password
Only allow emails ending in @org.com — reject anything else with 400
Hash password with bcryptjs before storing
Role defaults to EMP automatically — user never picks their role
If email already exists, return 400 with a clear message
On success return { status: 'success', data: { userId, name, email, role } }

POST /rest/onboardings/login

Request body: email, password
Only allow emails ending in @org.com — reject anything else with 400
Verify password against bcrypt hash
On success, sign a JWT containing { userId, email, role } using JWT_SECRET
Set the JWT as an httpOnly cookie named exactly token with sameSite: 'lax'
On success return { status: 'success', data: { userId, name, email, role } }

POST /rest/onboardings/logout

Clears the token cookie
Returns { status: 'success', data: { message: 'Logged out successfully' } }


ROLES MODULE — implement fully
POST /rest/roles/assign

Protected: only CFO can call this (use authenticate + authorize middleware)
Request body: userId, role
Allowed roles to assign: EMP, RM, APE only — assigning CFO role must be blocked with 400
If userId does not exist, return 404
If the role string is not one of the valid values, return 400
If the user already has that role, return 400 with message "User already has this role"
Role downgrade logic: if the user currently has role RM and is being changed to any other role, automatically delete all rows in employee_manager where rm_id matches that user's id. Those employees are now unassigned and can be reassigned to another RM by the CFO later.
On success return { status: 'success', data: { userId, name, email, role } } with the updated user


EMPLOYEES MODULE — implement fully
GET /rest/employees

Protected: EMP is not allowed — return 403 if an EMP calls this
RM: returns only the EMPs assigned to them (rows in employee_manager where rm_id = calling user's id), joined with users table
APE: returns all users with role EMP and all users with role RM
CFO: returns all users (every role including APE and CFO)
Response shape exactly:

json{
  "status": "success",
  "data": {
    "users": [
      { "userId": "...", "name": "...", "email": "...", "role": "..." }
    ]
  }
}
POST /rest/employees/assign

Protected: only CFO can call this
Request body: empId (the EMP's userId), rmId (the RM's userId)
Validate that the user with empId has role EMP — if not, return 400
Validate that the user with rmId has role RM — if not, return 400
If the EMP is already assigned to an RM, return 400 with message "Employee is already assigned to a reporting manager"
On success insert into employee_manager and return { status: 'success', data: { message: 'Employee assigned successfully' } }

DELETE /rest/employees/assign

Protected: only CFO can call this
Request body: empId, rmId
Validate both users exist and have correct roles
If the assignment does not exist, return 404 with message "Assignment not found"
On success delete the row and return { status: 'success', data: { message: 'Assignment removed successfully' } }


MIDDLEWARE — verify these are correctly wired:
authenticate.js — reads JWT from httpOnly cookie named token, verifies with JWT_SECRET, attaches decoded payload to req.user. Returns 401 if cookie missing or token invalid or expired.
authorize.js — factory function, takes array of allowed roles like ['CFO'] or ['CFO', 'RM']. Checks req.user.role. Returns 403 with { status: 'error', message: 'Forbidden' } if role not allowed.
errorHandler.js — catches AppError instances and returns their statusCode and message. For unexpected errors returns 500 with a safe generic message. Never expose stack traces in responses.

EDGE CASES to handle explicitly:

Any protected route called without a cookie → 401
Any route called with a valid cookie but wrong role → 403
POST /rest/roles/assign with role: 'CFO' → 400 with message "Cannot assign CFO role"
POST /rest/employees/assign where empId and rmId are the same user → 400 with message "Employee and manager cannot be the same user"
DELETE /rest/employees/assign where assignment doesn't exist → 404
All DB errors should be caught and passed to the global error handler via next(error)


Do NOT implement yet:

POST /rest/reimbursements
PATCH /rest/reimbursements
GET /rest/reimbursements
GET /rest/reimbursements/:userId

Leave those route stubs and controller/service functions exactly as they were — empty with TODO comments.


3rd Prompt:-
Now implement the full reimbursements module. The routes are already stubbed — fill in the complete business logic in the controller and service files.
Core rules before anything else:
A reimbursement (RI) can only be created by a user with role EMP. All other roles cannot create one. Once created, the EMP cannot edit it in any way.
A reimbursement has exactly three possible statuses: PENDING, REJECTED, APPROVED.
Every newly created RI starts with status PENDING.
DUAL APPROVAL RULE — this is the most important logic in the entire module:
A reimbursement is only marked APPROVED (visible to EMP as APPROVED) when ALL of these two conditions are met:

The RM to whom that EMP is assigned has approved it (rm_approved = true)
At least one APE has approved it (ape_approved = true)

Until both conditions are true, the status shown remains PENDING. The rm_approved and ape_approved boolean columns in the reimbursements table track this independently. When both flip to true, update status to APPROVED.
REJECTION RULE — once rejected it is final:
If any approver (RM, APE, or CFO acting as either gate) sets status to REJECTED, update status to REJECTED immediately and no further changes are allowed on that RI. Any subsequent attempt to approve or reject an already REJECTED RI must return 400 with message "Reimbursement has already been rejected".

POST /rest/reimbursements

Protected: only EMP role allowed
Request body: title, description, amount
Validate all three fields are present — return 400 if any missing
Validate amount is a positive number — return 400 if not
Insert into reimbursements table with emp_id from req.user.userId, status = 'PENDING', rm_approved = false, ape_approved = false
Also insert a row into reimbursement_approvals is NOT needed on creation — only on approval/rejection actions
Return { status: 'success', data: { reimbursementId, title, description, amount, status: 'PENDING' } }


PATCH /rest/reimbursements

Protected: RM, APE, CFO allowed — EMP gets 403
Request body: userId (the EMP who owns the RI), status (either 'APPROVED' or 'REJECTED')
Validate status is exactly 'APPROVED' or 'REJECTED' — return 400 otherwise
Find the reimbursement(s) for that userId that are not yet fully resolved. If multiple pending RIs exist for that EMP, apply the action to all of them that are still actionable by this approver role. If none found return 404 with "No actionable reimbursement found for this user"

Logic by role:
If caller is RM:

Verify the EMP (userId) is actually assigned to this RM in employee_manager — if not, return 403 with "You are not the reporting manager for this employee"
If status = 'REJECTED': set status = 'REJECTED' on the RI, insert row into reimbursement_approvals with approver_role = 'RM', decision = 'REJECTED'
If status = 'APPROVED': set rm_approved = true, insert approval row. Then check if ape_approved is also already true — if yes, set status = 'APPROVED'

If caller is APE:

APE can act on any RI where rm_approved = true and status is still PENDING
If status = 'REJECTED': set status = 'REJECTED', insert approval row with approver_role = 'APE', decision = 'REJECTED'
If status = 'APPROVED': set ape_approved = true, insert approval row. Then check if rm_approved is also already true — if yes, set status = 'APPROVED'

If caller is CFO:

CFO follows the same dual-approval flow — does NOT bypass RM + APE gates
CFO acts based on what stage the RI is at:

If rm_approved = false: CFO action counts as the RM gate — update rm_approved accordingly
If rm_approved = true and ape_approved = false: CFO action counts as the APE gate — update ape_approved accordingly
If status = 'REJECTED': return 400 with "Reimbursement has already been rejected"
Insert approval row with approver_role = 'CFO'
After each CFO action check if both gates are now true and update status = 'APPROVED' if so


Return { status: 'success', data: { message: 'Reimbursement status updated' } }


GET /rest/reimbursements
Protected: all roles allowed but visibility differs by role.
If caller is EMP:

Return ALL their own RIs regardless of status (PENDING, APPROVED, REJECTED)
Filter: reimbursements.emp_id = req.user.userId

If caller is RM:

Return only RIs that are PENDING (not yet fully resolved) from EMPs assigned to this RM
Join employee_manager on emp_id where rm_id = req.user.userId
Filter: status = 'PENDING'

If caller is APE:

Return RIs where rm_approved = true AND status = 'PENDING' (approved by RM but not yet by APE)
These are the ones waiting at the APE gate

If caller is CFO:

Return RIs where ape_approved = true AND status = 'APPROVED' (fully approved by both gates)

Response shape exactly:
json{
  "status": "success",
  "data": {
    "reimbursements": [
      {
        "title": "...",
        "description": "...",
        "amount": 999,
        "status": "..."
      }
    ]
  }
}

GET /rest/reimbursements/:userId

Protected: RM, APE, CFO allowed — EMP gets 403
Lists ALL reimbursements for the given :userId
The target user must have role EMP — if not return 400 with "Target user is not an employee"
RM: can only view if the target EMP is assigned to them in employee_manager — if not return 403 with "This employee is not under your management"
APE: can view any EMP's RIs
CFO: can view any EMP's RIs
Same response shape as above


Audit log — reimbursement_approvals table:
Every approval or rejection action must insert a row into reimbursement_approvals with:

reimbursement_id — the RI being acted on
approver_id — req.user.userId
approver_role — req.user.role
decision — 'APPROVED' or 'REJECTED'
decided_at — current timestamp


Edge cases to handle explicitly:

EMP tries to call PATCH /rest/reimbursements → 403
EMP tries to call GET /rest/reimbursements/:userId → 403
RM tries to approve an EMP not assigned to them → 403
Any approver tries to act on an already REJECTED RI → 400
userId in PATCH body does not exist → 404
amount is negative or zero → 400
Missing title, description, or amount in POST body → 400
RI not found for given userId in PATCH → 404
Target user in GET /:userId is not an EMP → 400
All DB errors caught and passed to global error handler via next(error)


Do not change anything outside the reimbursements module — auth, roles, employees, middlewares stay exactly as they are.


4th Prompt :- bug fix 

Make the following targeted changes to the reimbursements module only. Do not touch any other module, middleware, or configuration.
Context for why this change is needed:

The current PATCH /rest/reimbursements only accepts userId and status. If an EMP has raised multiple reimbursements, there is no way to target a specific one. The fix is to include reimbursementId in both the GET response (so approvers can see which ID to reference) and the PATCH request body (so they can target exactly one RI).

CHANGE 1 — GET /rest/reimbursements response shape
Add reimbursementId as the first field in every reimbursement object returned, for ALL roles (EMP, RM, APE, CFO). The updated response shape must be exactly:
json{
  "status": "success",
  "data": {
    "reimbursements": [
      {
        "reimbursementId": "uuid-here",
        "title": "...",
        "description": "...",
        "amount": 999,
        "status": "..."
      }
    ]
  }
}
This applies to every role's query in the GET handler — EMP sees their own RIs with reimbursementId, RM sees their pending RIs with reimbursementId, APE sees RM-approved pending RIs with reimbursementId, CFO sees fully approved RIs with reimbursementId. No role is excluded from seeing the reimbursementId.

CHANGE 2 — GET /rest/reimbursements/:userId response shape
Same change — add reimbursementId as the first field in every reimbursement object returned here as well. Exact same shape as above.

CHANGE 3 — PATCH /rest/reimbursements request body
The PATCH endpoint now accepts three fields in the request body:

userId — the EMP who owns the RI (already existed)
reimbursementId — the specific RI being acted on (new, required)
status — either 'APPROVED' or 'REJECTED' (already existed)

Validation to add at the top of the PATCH service function before any role logic runs:

If reimbursementId is missing from request body → return 400 with message "reimbursementId is required"
If userId is missing → return 400 with message "userId is required"
If status is missing or not exactly 'APPROVED' or 'REJECTED' → return 400 with message "status must be APPROVED or REJECTED"
Look up the reimbursement by reimbursementId in the DB — if not found → return 404 with message "Reimbursement not found"
Validate that the found reimbursement's emp_id matches the provided userId — if they do not match → return 400 with message "Reimbursement does not belong to this user"
If the reimbursement's current status is already 'REJECTED' → return 400 with message "Reimbursement has already been rejected"

Only after all six validations pass should the role-based approval logic execute. The role logic itself (RM gate, APE gate, CFO flow, dual approval check) remains exactly the same as before — only the targeting changes from "find by userId" to "find by reimbursementId, then validate userId matches".

CHANGE 4 — PATCH service internal query change
Previously the service was finding the RI by emp_id = userId. Now it must:

Find the specific RI by id = reimbursementId (single row lookup, not a bulk query)
Then verify emp_id = userId as a validation step (covered in validation above)
Then proceed with the same role-based approval logic on that single RI

Remove any logic that was applying the action to multiple RIs at once. Each PATCH call now targets exactly one RI identified by its reimbursementId.

Do not change anything else:

Do not touch POST /rest/reimbursements
Do not touch GET /rest/reimbursements filter logic — only add reimbursementId to the response shape
Do not touch GET /rest/reimbursements/:userId filter logic — only add reimbursementId to the response shape
Do not touch auth, roles, employees, middlewares, config, or seed files
Do not change the dual approval logic, rejection finality rule, or role visibility rules


After applying these changes, the correct test flow for multiple RIs is:

EMP creates RI #1 and RI #2
EMP calls GET /rest/reimbursements → sees both with their unique reimbursementIds
RM calls GET /rest/reimbursements → sees both pending RIs with their reimbursementIds
RM calls PATCH with { userId, reimbursementId: "id-of-RI-1", status: "APPROVED" } → only RI #1 is affected
RI #2 remains untouched and still PENDING
APE calls GET /rest/reimbursements → sees only RI #1 (rm_approved=true), not RI #2
APE calls PATCH with { userId, reimbursementId: "id-of-RI-1", status: "APPROVED" } → RI #1 is now fully APPROVED
EMP calls GET /rest/reimbursements → sees RI #1 as APPROVED, RI #2 still PENDING


5th Prompt :-
1. CFO GET — currently only returns fully APPROVED RIs. Needs to also return REJECTED ones for audit. Fix the query to return where status = 'APPROVED' OR status = 'REJECTED'.
2. Duplicate approval block — if RM calls PATCH APPROVED on an RI where rm_approved is already true, return 400 with "You have already approved this reimbursement". Same for APE if ape_approved is already true.

6th prompt - Refactoring 
Refactor ALL authorization checks across ALL modules (auth, roles, employees, reimbursements) to use a unified Policy/Strategy pattern. The goal: every role's permissions live in one policy file, services become thin orchestrators, and adding a role takes 30 minutes instead of a day.
Architecture:
Create a policies folder at the root of src/modules. Each module will reference these shared policies, not define its own. One policy per role, with methods for every action: canView(), canCreate(), canUpdate(), canDelete(), canAssign(), etc.
Folder structure — create these:
src/
  modules/
    policies/                    ← NEW: shared across all modules
      RolePolicy.js             ← handles all RM permissions
      APEPolicy.js              ← handles all APE permissions
      CFOPolicy.js              ← handles all CFO permissions
      helpers.js                ← shared DB/audit logic
      index.js                  ← exports all policies
    auth/
      auth.service.js           ← refactored (almost no auth checks)
      auth.routes.js            ← unchanged
    roles/
      roles.service.js          ← refactored (uses CFOPolicy)
      roles.routes.js           ← unchanged
    employees/
      employees.service.js      ← refactored (uses policies for visibility + actions)
      employees.routes.js       ← unchanged
    reimbursements/
      reimbursements.service.js ← heavily refactored (delegates all logic to policies)
      reimbursements.routes.js  ← unchanged

Policy file structure — each role policy has these methods:
Every policy is a JavaScript object with methods. Methods throw AppError if not allowed, or return data/boolean. No if/else inside services — policies decide everything.
js// Example: src/modules/policies/RMPolicy.js
module.exports = {
  // --- Employees module ---
  canViewEmployees() {
    // RM can always view their team
    return true
  },

  async getVisibleEmployees(callerId) {
    // Returns only EMPs assigned to this RM
    // Called by employees.service during GET
  },

  // --- Reimbursements module ---
  async canViewReimbursement(ri, callerId) {
    // RM can see a RI if the EMP is assigned to them
    // Return boolean or throw
  },

  async getVisibleReimbursements(callerId) {
    // Returns all PENDING RIs from assigned EMPs
  },

  async canApproveReimbursement(ri, callerId, newStatus) {
    // Check: is EMP assigned to this RM?
    // Check: is RI still actionable?
    // Check: has RM already approved?
    // Throw if any check fails
  },

  async approveReimbursement(ri, callerId, newStatus) {
    // Do the actual DB update
    // Insert into reimbursement_approvals
    // Return result
  },

  // --- Roles module ---
  canAssignRole() {
    throw new AppError('Forbidden', 403) // RM cannot assign roles
  }
}

What each role's policy must implement:
RMPolicy.js methods:

canViewEmployees() — always true
getVisibleEmployees(callerId) — EMPs assigned to this RM
canViewReimbursements() — can see, returns true
getVisibleReimbursements(callerId) — PENDING from their EMPs
canViewReimbursement(ri, callerId) — can see specific RI if EMP is theirs
canApproveReimbursement(ri, callerId, status) — check RM assignment, actionability, already approved
approveReimbursement(ri, callerId, status) — set rm_approved, check dual gate, insert audit log
canAssignRole() — throw 403

APEPolicy.js methods:

canViewEmployees() — true
getVisibleEmployees() — all EMPs and RMs
canViewReimbursements() — true
getVisibleReimbursements() — RIs where rm_approved = true AND status = PENDING
canViewReimbursement(ri) — can see if rm_approved = true
canApproveReimbursement(ri, callerId, status) — check rm_approved = true, not already ape_approved, status is PENDING
approveReimbursement(ri, callerId, status) — set ape_approved, check dual gate, insert audit log
canAssignRole() — throw 403

CFOPolicy.js methods:

canViewEmployees() — true
getVisibleEmployees() — all users
canViewReimbursements() — true
getVisibleReimbursements() — APPROVED and REJECTED (audit log)
canViewReimbursement(ri) — always true
canApproveReimbursement(ri, callerId, status) — check status !== REJECTED, decide which gate to fill
approveReimbursement(ri, callerId, status) — fill missing gate, check dual gate, insert audit log
canAssignRole(targetRole) — allow except if targetRole === 'CFO'
assignRole(userId, role) — update user role, if downgrading RM then delete employee_manager rows

EMPPolicy.js methods:

canViewEmployees() — throw 403
canViewReimbursements() — true
getVisibleReimbursements(callerId) — all their own RIs
canViewReimbursement(ri, callerId) — true if ri.emp_id === callerId
canCreateReimbursement() — true
createReimbursement(title, description, amount, empId) — insert RI
canApproveReimbursement() — throw 403
canAssignRole() — throw 403


src/modules/policies/helpers.js — shared functions:
These are called by policies, not services. They handle DB operations that multiple policies need:
jsconst db = require('../../config/db');
const { reimbursements, reimbursementApprovals, employeeManager } = require('../../db/schema');
const AppError = require('../../utils/AppError');

// Get all RIs for an RM's team
async function getRMTeamReimbursements(rmId) { ... }

// Get all RMs's assigned EMPs
async function getRMAssignedEmployees(rmId) { ... }

// Check if EMP is assigned to this RM
async function isEMPAssignedToRM(empId, rmId) { ... }

// Update RI approval flags and status
async function updateReimbursementApproval(riId, updates) { ... }

// Insert audit log entry
async function logApproval(riId, approverId, role, decision) { ... }

// Handle RM downgrade (delete employee_manager rows)
async function removeRMAssignments(rmId) { ... }

// Get user by ID
async function getUser(userId) { ... }

// Get RI by ID with full context
async function getReimbursement(riId) { ... }

module.exports = { getRMTeamReimbursements, ... }

src/modules/policies/index.js — exports all policies:
jsconst RMPolicy = require('./RolePolicy');
const APEPolicy = require('./APEPolicy');
const CFOPolicy = require('./CFOPolicy');
const EMPPolicy = require('./EMPPolicy');

module.exports = {
  'RM': RMPolicy,
  'APE': APEPolicy,
  'CFO': CFOPolicy,
  'EMP': EMPPolicy,
};

How services use policies — example from reimbursements.service.js:
jsconst policies = require('../policies');

const updateReimbursement = async ({ userId, reimbursementId, status, callerId, callerRole }) => {
  // 1. Validate inputs (no role checks here)
  if (!reimbursementId || !userId || !['APPROVED', 'REJECTED'].includes(status)) {
    throw new AppError('Invalid input', 400);
  }

  // 2. Fetch the RI
  const ri = await helpers.getReimbursement(reimbursementId);
  if (!ri) throw new AppError('Reimbursement not found', 404);
  if (ri.emp_id !== userId) throw new AppError('RI does not belong to this user', 400);

  // 3. Get the policy for this role
  const policy = policies[callerRole];
  if (!policy) throw new AppError('Forbidden', 403);

  // 4. Policy checks if allowed (throws if not)
  await policy.canApproveReimbursement(ri, callerId, status);

  // 5. Policy executes the update
  await policy.approveReimbursement(ri, callerId, status);

  return { message: 'Reimbursement status updated' };
};
That's it. 8 lines of actual logic. No if/else. No role checks in the service.

How GET endpoints use policies — example from employees.service.js:
jsconst getEmployees = async ({ callerRole, callerId }) => {
  const policy = policies[callerRole];
  if (!policy) throw new AppError('Forbidden', 403);

  // Policy decides visibility
  const users = await policy.getVisibleEmployees(callerId);

  return users;
};

Refactoring checklist — do NOT change route files:
✓ Create src/modules/policies/ folder and all policy files

✓ Create src/modules/policies/helpers.js with shared DB functions

✓ Create src/modules/policies/index.js that exports all policies

✓ Refactor src/modules/auth/auth.service.js — remove role checks (register/login have none, logout just clears cookie)

✓ Refactor src/modules/roles/roles.service.js — delegate to CFOPolicy

✓ Refactor src/modules/employees/employees.service.js — use policies for both visibility and authorization

✓ Refactor src/modules/reimbursements/reimbursements.service.js — delegate all role logic to policies

✓ Every service method becomes 5-10 lines: validate → fetch → get policy → policy.can*() → policy.execute()

✓ DO NOT touch route files, middleware, config, or schema

✓ Add prompt-logs.txt entries documenting which prompt led to each policy file

Testing strategy after refactor:
Each policy should have unit tests. Example:
js// test RMPolicy.approveReimbursement
const ri = { id: '...', emp_id: emp1, rm_approved: false, ape_approved: false };
await RMPolicy.canApproveReimbursement(ri, rm1);  // should throw (RM not assigned)
const result = await RMPolicy.approveReimbursement(ri, rm1);  // should update DB

This is a full refactor of your entire authorization layer. After this runs, your codebase is ready for production and can scale: add a new role in 30 minutes, change a permission in one policy file, test roles in isolation.

================================================================================
7th Prompt :- (2026-06-28)
Refactor ALL authorization checks across ALL modules (auth, roles, employees,
reimbursements) to use a unified Policy/Strategy pattern.

Goal: every role's permissions live in one policy file, services become thin
orchestrators, and adding a role takes 30 minutes instead of a day.

Files created:
  src/modules/policies/helpers.js      — shared DB/audit utility functions
  src/modules/policies/RMPolicy.js     — all RM permissions and actions
  src/modules/policies/APEPolicy.js    — all APE permissions and actions
  src/modules/policies/CFOPolicy.js    — all CFO permissions and actions
  src/modules/policies/EMPPolicy.js    — all EMP permissions and actions
  src/modules/policies/index.js        — exports all policies keyed by role string

Files refactored (services now ~10-line orchestrators, zero if/else role checks):
  src/modules/auth/auth.service.js           — policy-agnostic by design (no changes)
  src/modules/roles/roles.service.js         — delegates to CFOPolicy.canAssignRole/assignRole
  src/modules/employees/employees.service.js — getEmployees delegates to policy.getVisibleEmployees
  src/modules/reimbursements/reimbursements.service.js — all CRUD delegates to policy methods

Files updated (non-route):
  src/modules/roles/roles.controller.js — now forwards callerRole from JWT payload

Pattern per service method:
  1. Validate inputs
  2. Fetch the resource (RI, user, etc.)
  3. Resolve policy: const policy = policies[callerRole]
  4. policy.can*() — throws AppError if forbidden
  5. policy.execute() — performs DB updates + audit log
  6. Return result

Architecture benefit: Adding a new role = create one new *Policy.js + add one
line to index.js. No service files need to change.
================================================================================

================================================================================
8th Prompt :- (2026-06-28)
Clean up all service files to remove redundant functions left over from the
Policy/Strategy refactor. Services should contain ONLY thin orchestrator methods
that controllers call.

Changes made:
  auth.service.js
    - Confirmed: 2 functions only (registerUser, loginUser)
    - logout is handled entirely in the controller (clear cookie); no service needed
    - Removed verbose block comments from 7th prompt refactor; clean final state

  roles.service.js
    - Already clean from 7th prompt; 1 function (assignRole). No changes needed.

  employees.service.js
    - BEFORE: ~146 lines with raw DB queries in assignManager and removeManager
    - AFTER: ~50 lines, zero raw DB queries
    - Moved all validation + DB logic into CFOPolicy.canAssignManager/assignManager
      and CFOPolicy.canRemoveAssignment/removeAssignment
    - Service now: UUID validate → get policy → policy.can*() → policy.execute()

  reimbursements.service.js
    - Already thin from 7th prompt; minor cleanup:
    - createReimbursement reduced to 1-line delegation
    - getReimbursements stripped to 3 lines
    - getReimbursementsByUser: only remaining raw DB query is structural
      user-existence check (not role logic)

Policy files updated:
  policies/CFOPolicy.js — added canAssignManager, assignManager,
                          canRemoveAssignment, removeAssignment
  policies/helpers.js   — added empHasManager, insertManagerAssignment,
                          deleteManagerAssignment

Final function count per service:
  auth.service.js:            2 functions (registerUser, loginUser)
  roles.service.js:           1 function  (assignRole)
  employees.service.js:       3 functions (getEmployees, assignManager, removeManager)
  reimbursements.service.js:  4 functions (createReimbursement, updateReimbursement,
                                            getReimbursements, getReimbursementsByUser)
  Total: 10 functions across all services
================================================================================